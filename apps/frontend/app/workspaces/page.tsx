"use client";import{useEffect,useState}from"react";import Shell from"@/components/Shell";import{Card,PageHeader}from"@/components/Page";import{api}from"@/lib/api";export default function Workspaces(){const[x,setX]=useState<any[]>([]),[name,setName]=useState("");const load=()=>api<any[]>("/workspaces").then(setX);useEffect(()=>{void load()},[]);async function add(){await api("/workspaces",{method:"POST",body:JSON.stringify({name,description:"New governed knowledge workspace"})});setName("");await load()}return <Shell><PageHeader eyebrow="Structure" title="Workspaces" description="Organize knowledge and decisions by accountable business context."/><Card><div className="row"><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Workspace name"/><button className="btn primary" onClick={add}>Create</button></div></Card><div className="list" style={{marginTop:16}}>{x.map(w=><Card key={w.id}><strong>{w.name}</strong><p className="muted">{w.description}</p></Card>)}</div></Shell>}
