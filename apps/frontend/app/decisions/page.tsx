"use client";import{useEffect,useState}from"react";import Shell from"@/components/Shell";import{Card,PageHeader}from"@/components/Page";import{api}from"@/lib/api";export default function Decisions(){const[x,setX]=useState<any[]>([]),[ws,setWs]=useState<any[]>([]),[title,setTitle]=useState("Supplier approval"),[question,setQuestion]=useState("Should we approve Vendor X?"),[workspace,setWorkspace]=useState("");async function load(){const[d,w]=await Promise.all([api<any[]>("/decisions"),api<any[]>("/workspaces")]);setX(d);setWs(w);if(!workspace&&w[0])setWorkspace(w[0].id)}useEffect(()=>{void load()},[]);async function create(){await api("/decisions",{method:"POST",body:JSON.stringify({workspace_id:workspace,title,question})});await load()}return <Shell><PageHeader eyebrow="Decide" title="Decision Center" description="Turn governed evidence into accountable recommendations and retained decision memory."/><Card><div className="list"><select className="input" value={workspace} onChange={e=>setWorkspace(e.target.value)}>{ws.map(w=><option value={w.id} key={w.id}>{w.name}</option>)}</select><input className="input" value={title} onChange={e=>setTitle(e.target.value)}/><textarea className="input textarea" value={question} onChange={e=>setQuestion(e.target.value)}/><button className="btn primary" onClick={create}>Create analysis</button></div></Card><div className="list" style={{marginTop:16}}>{x.map(d=><Card key={d.id}><div className="row between"><strong>{d.title}</strong><span className="badge">{Math.round(d.confidence*100)}% confidence</span></div><p className="muted">{d.question}</p><p>{d.recommendation}</p></Card>)}</div></Shell>}
