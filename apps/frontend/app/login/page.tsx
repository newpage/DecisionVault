"use client";import {useState} from "react";import {API} from "@/lib/api";export default function Login(){const[tenant,setTenant]=useState("acme"),[email,setEmail]=useState("demo@decisionvault.ai"),[password,setPassword]=useState("DecisionVault!"),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setError("");const r=await fetch(`${API}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({tenant,email,password})});if(!r.ok){setError("Unable to sign in");return}const data=await r.json();localStorage.setItem("dv_token",data.access_token);location.href="/dashboard"}return <div className="login"><form className="card login-card" onSubmit={submit}><div className="eyebrow">DecisionVault</div><h1>Welcome back</h1><p className="muted">Access trusted knowledge and decision intelligence.</p><div className="list"><input className="input" value={tenant} onChange={e=>setTenant(e.target.value)} placeholder="Tenant"/><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/>{error&&<div className="danger">{error}</div>}<button className="btn primary">Sign in</button></div></form></div>}
