# Sprint 3 — RUP-3.1 Business Concepts

## Delivered

- Tenant-scoped Business Concept model.
- Optional Knowledge Card relationship to a Business Concept.
- Business Concepts API with search and connected Knowledge Card counts.
- Six seeded business concepts.
- Existing supplier knowledge linked to Supplier Qualification.
- Business Concepts navigation and responsive visual explorer.
- Service-level unit test.

## API

`GET /api/v1/business-concepts`

Optional query:

`GET /api/v1/business-concepts?q=validation`

## Database

Breaking schema change. Use the current fresh-database deployment:

```bash
dv deploy --reset-db
```

If the local `dv` wrapper does not pass arguments yet:

```bash
cd /opt/decisionvault/DecisionVault
./scripts/server-deploy.sh --reset-db
```
